# Segredo — nada disso entra no repositório
.env
.env.*
*.key
*.keystore
*.jks
google-services.json
key.properties

# Config de build: o `.example` é versionado, o real fica local (carrega a
# anon key, e chave em repositório é hábito que uma hora erra o arquivo).
app/config/*.json
!app/config/*.json.example

# Dart / Flutter
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
*.g.dart
.puro.json

# Node / NestJS
node_modules/
dist/

# Ferramentas locais
.code-review-graph/
.claude/worktrees/
.claude/RESUME.md
.claude/settings.local.json
.claude/RESUME.md
.idea/
*.iml
infra/local/.runtime.env

# Evidências pesadas de E2E ficam versionadas (são a prova); logs temporários não
*.log
criar-conta.py
