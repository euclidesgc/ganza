# RETOMADA

- **Item em curso**: 002 - Conta, configurações e chaves do usuário — **Fase 5 (Integração bancária)**. **Cinco de seis tarefas em `CUMPRIDO`**: T5.1, T5.2, T5.3, T5.4 e T5.5. Só a **T5.6** (tela de `/configuracoes/banco`) está em execução. A Fase 4 fechou e a pilha #31/#32 já foi mergeada em `develop` (`1250e7d`).
- **A pendência humana P13 caiu.** As credenciais da Pluggy foram entregues pelo humano, gravadas em `infra/local/.env` (permissão `600`, ignorado pelo git) nos três worktrees, e validadas: `POST /auth` da Pluggy devolveu `200`. **O humano vai rotacionar o par quando a fase fechar** — avisar. Não há mais nada bloqueado por ele.
- **Branches / worktrees**: pilha local montada e **não pushada** — `feature/GZ-30-schema-conexoes-bancarias` em `.claude/worktrees/gz-30` (só a migration `0009`, **PR 5a**) e `feature/GZ-31-integracao-bancaria` em `.claude/worktrees/gz-31`, rebaseada sobre a GZ-30, com todo o resto (**PR 5b**).
- **Primeira ação da retomada**: conferir se o executor da **T5.6** ainda está vivo (`/clear` não mata sub-agente) e qual o topo da `GZ-31`. Com a T5.6 em `CUMPRIDO`, o próximo passo é a skill **`fechar-etapa`**, que verifica o DoD da Fase 5 rodando, e só então abrir a pilha de PRs com `gh stack`.
- **Depois do PR**: QA e CISO em paralelo, `critico-integrador` sobre as fatias consolidadas, e então a bateria automatizada (`escrever-testes`) e as docs (`manter-docs-vivas`), cada frente em paralelo.
- **O que esta fase gastou e por quê**: **quinze** correções de DoD antes ou durante o despacho — **CHG-026 a CHG-040**. Nenhuma afrouxou exigência. O padrão que se repetiu, e que vale carregar para a Fase 6: **critério que descreve o ambiente em vez de construí-lo falha** (`psql` sem host, `flutter analyze` sem `cd app`, `.runtime.env` ausente em worktree novo); **grep negativo sobre árvore povoada é verde por construção**; **texto de política do Postgres depende do `search_path` de quem lê**; e um padrão de varredura de segredo chegou a **casar a própria linha do critério**.
- **Armadilhas de ambiente já pagas** (repassar a todo executor): política de RLS lida com `-U supabase_admin` sai sem o prefixo `auth.` — use `-e PGOPTIONS=--search_path=public`; a stack Docker local é **única e compartilhada** entre os três worktrees, e `local-supabase.sh reset` faz `down -v`; `infra/local/.runtime.env` só nasce com `local-supabase.sh up` e não existe em worktree novo; o contêiner `functions` **não tem `curl`** e só recebe variável nova ao ser **recriado** (`up -d`, não `restart`); heredoc para `docker exec -i psql` chega vazio; Postgres na **54322**; em worktree, histórico só por `rtk proxy git`; `flutter test -r compact` usa `\r`, passe por `tr '\r' '\n'`.
- **Decisões desta fase**: **FD-034** — o `503 missing_configuration` chega à tela como `UnexpectedFailure` com mensagem, **não** como tipo próprio, porque `Failure` é `sealed` e um caso novo quebraria três banners fora da fatia; a tela distingue por string, e a condição que reabre está registrada. **CHG-031** — a T5.3 foi antecipada à T5.2 de propósito.
- **GitGuardian**: achado se resolve **no dashboard**, nunca no `.gitguardian.yaml` (**P7**). O hook `pre-commit` acusa os JWT demo do Supabase local em `infra/local/docker-compose.yml`; é falso positivo conhecido e o escape é `GANZA_ALLOW_SECRET=1`.
- **Ponteiros**: `docs/002_conta_e_configuracoes/03_plan.md` (Fase 5 na linha 1404; DoD da fase logo após a T5.6) · `changes.md` (CHG-026 a CHG-040) · `decisions.md` (**FD-034**) · provas em `docs/002_conta_e_configuracoes/provas/t5_*.md`.
