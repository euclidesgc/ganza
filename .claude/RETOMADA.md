# RETOMADA

- **Estado**: 002 — **Fase 5 (Integração bancária) MERGEADA em `develop`** (`68e00fb`), pilha #34/#35, CI verde, CISO `pass`, seis tarefas `CUMPRIDO`. Worktrees da fase removidos e 13 branches órfãs apagadas. **Sobrou `docs/GZ-9-fechar-f08` no servidor** — tem 1 commit não integrado por patch-id, é pendência do humano, não apagar sem decisão.
- **Sessão em curso**: **pausa para organizar o harness**, a pedido do humano (ele volta à noite). O trabalho está na branch `chore/GZ-32-harness-pos-fase-5`, **não mergeada**, com as branches `chore/GZ-33-lint-dod` (worktree `.claude/worktrees/wt-lint`) e `chore/GZ-34-hooks-formato` (worktree `.claude/worktrees/wt-hooks`, **pronta**, commit `20d1687`) para integrar.
- **Primeira ação da retomada**: conferir se o agente do `scripts/lint-dod.sh` (worktree `wt-lint`) terminou; então integrar `GZ-33` e `GZ-34` na `GZ-32`, rodar a cancela de máquina inteira, abrir PR, **mergear** e apagar branches e worktrees.
- **APK entregue**: `build-artifacts/ganza-prod-hml-2026-08-21-68e00fb.apk` (53 MB), flavor `prod`, apontando para a **HML**, assinado com a **chave de debug** — atualização futura exige a mesma chave ou desinstalar antes. `app/config/hml.json` foi criado com a anon key vinda da API do Coolify e é ignorado pelo git.
- **Cinco decisões novas do humano (21/08/2026), já aplicadas no `CLAUDE.md` e na memória**: higiene de código (format aplicado, console sem erro/warning/lint) é **DoD geral**; produção não muda para teste passar; **PR e merge são do agente** com a bateria verde, apagando branch e worktree no mesmo movimento; **E2E é do humano, sob demanda**; e o corte de entrega é **por família de prova**, não por contagem de tarefas.
- **O achado que mais importou**: a tarefa **TL.4** mandava apagar `app/patrol_test/` e a dependência `patrol` — os dois existem. Com o E2E voltando a ser ferramenta do humano, executá-la destruiria o que ele usa. **Cancelada (CHG-042)**, com a **D34 revisada**.
- **Diagnóstico do custo, medido**: a auditoria de critério acertou 16/16 na Fase 5 e deixou passar zero `DOD INVÁLIDO` — o gargalo é a **escrita** do critério, não o gate. Fase 5: 6 tarefas, **2,67** correções por tarefa, **seis famílias de prova**. Fase 1: 16 tarefas, **0,69**, poucas famílias repetidas. Menos tarefas não é entrega menor; trocar de família a cada tarefa é o que encarece.
- **Já corrigido no harness**: gabarito `docs/_templates/feature/03_plan.md` (fabricava os defeitos de CHG-019/027/034/039); higiene fora do DoD de tarefa; 10 armadilhas nomeadas no `tech-lead`; auditor devolve todas as classes de uma vez e reaudita o bloco inteiro; `empilhar-prs` dizia "Merge (é do humano)"; E2E reconciliado em 5 arquivos; "produção não muda para teste passar" em `qa`, `supervisor-dod` e `escrever-testes`; format-on-write e `verify-gauntlet --changes` no `PostToolUse`; `pre-commit` com cancela de formato.
- **Armadilhas de ambiente** (repassar a todo executor): política de RLS lida com `-U supabase_admin` sai sem o prefixo `auth.` — use `-e PGOPTIONS=--search_path=public`; a stack Docker local é única e compartilhada entre worktrees; `infra/local/.runtime.env` só nasce com `local-supabase.sh up` e não existe em worktree novo; o contêiner `functions` **não tem `curl`** e só pega variável nova ao ser **recriado**; heredoc para `docker exec -i psql` chega vazio; Postgres na **54322**; em worktree, histórico só por `rtk proxy git`; `flutter test -r compact` usa `\r`.
- **Ponteiros**: `docs/002_conta_e_configuracoes/changes.md` (CHG-026 a CHG-042) · `docs/decisions.md` (**D34** revisada) · a retrospectiva completa está no corpo desta sessão, e as propostas ainda não aplicadas são o `lint-dod.sh` Tier B e os gabaritos de DoD por família de prova.
