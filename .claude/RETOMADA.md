# RETOMADA

- **Item em curso**: 002 - Conta, configurações e chaves do usuário — **Fase 4** (Configuração de IA e o gating), **12 de 13 tarefas CUMPRIDO**. Falta só a **T4.11** (gating), linha 1253 de `docs/002_conta_e_configuracoes/03_plan.md`.
- **Branch / PR**: `feature/GZ-29-configuracao-de-ia` (PR 4b, contém a `GZ-28`) e `feature/GZ-28-schema-credenciais-de-ia` (PR 4a, as duas migrations) — publicadas, **PRs ainda não abertos**; nascem depois do `fechar-etapa`.
- **Último gate/veredito**: T4.10 `CUMPRIDO`; a guarda `isClosed` foi provada empiricamente pelo supervisor (removeu a guarda, teste falhou com `Cannot emit new states after calling close`).
- **Primeira ação da retomada**: a **T4.11 estava em execução e foi interrompida por limite de sessão**, com trabalho **não commitado** preservado em `.claude/worktrees/t4-11` — `chat_module/`, `core/widgets/feedback/`, `chat_drawer_item.dart`, `app_router.dart`, `ganza_drawer.dart` e `app_router_gate_test.dart` (este último incompleto: parou ao montar as três transições). Conferir `git -C .claude/worktrees/t4-11 status --porcelain` e **redespachar `especialista-infra`** com o bloco DoD das linhas 1256-1261, mandando continuar de onde parou.
- **Depois**: `supervisor-dod` na T4.11 → skill `fechar-etapa` (verifica o DoD da fase rodando) → PRs 4a e 4b **em pilha** (`docs/GITFLOW.md` §6).
- **Pendências do humano**: **nenhuma**. A restrição "nenhuma chave real de IA em produção" caiu — a chave-mestra do Vault já estava em volume na produção (**FD-032**, medido na VPS; **CHG-020** reconciliou X5/X16/P12 e revogou a FD-018, que afirmava a premissa antiga).
- **Decisão em vigor desta fase**: **FD-033** — `/chat` vira rota registrada com tela mínima. O motivo não é custo: sem destino registrado o `redirect` não tem para onde desviar e a prova das três transições é inescrevível. O placeholder subiu para `app/lib/core/widgets/feedback/` (Gate 2), servindo dois módulos.
- **Armadilhas de ambiente já pagas** (repassar a todo executor): heredoc para `docker exec -i psql` chega **vazio** e sai `exit 0` sem executar; `git log` cru mostra parentesco errado em worktree, use `rtk proxy git`; há Postgres de outro projeto na porta **5432**, o do ganza está na **54322** — todo `psql` vai por `docker compose -f infra/local/docker-compose.yml exec -T db`.
- **Risco conhecido**: literal `sk-test-ganza-0008-ABCD` (fake) em `provas/t4_2_falha_sem_a_mudanca.md` e no `03_plan.md`. O hook local não barra; o GitGuardian roda no PR. Se falhar lá, tratar **sem** reescrever evidência já validada.
- **Ponteiros**: `docs/002_conta_e_configuracoes/03_plan.md` (Fase 4; ondas na tabela ao fim) · `decisions.md` (FD-032, FD-033) · `changes.md` (CHG-020, CHG-021, CHG-022) · `docs/decisions.md` (**D19 resolvida**)
