# RETOMADA

- **Item em curso**: 002 - Conta, configurações e chaves do usuário — **Fase 5 (Integração bancária)**. A **onda 1 fechou**: **T5.1** e **T5.4** em `CUMPRIDO` pelo `supervisor-dod`, registrados no `03_plan.md`. A **T5.2 não foi despachada** — depende de pendência humana. A Fase 4 fechou e a pilha #31/#32 **já está mergeada** em `develop` (`1250e7d`); não há PR aberto.
- **Branches / worktrees**: pilha local já montada — `feature/GZ-30-schema-conexoes-bancarias` em `.claude/worktrees/gz-30` (só a migration `0009`, vira o **PR 5a**) e `feature/GZ-31-integracao-bancaria` em `.claude/worktrees/gz-31`, **rebaseada sobre a GZ-30**, com o domain e as docs (**PR 5b**). Nada foi pushado ainda.
- **Último gate/veredito**: `CUMPRIDO` nas duas tarefas, com o supervisor rodando as provas do zero. Antes disso, quatro correções de DoD — **CHG-026 a CHG-029** —, todas de forma, nenhuma afrouxando exigência.
- **Primeira ação da retomada**: conferir se o auditor do bloco DoD da **T5.3** já devolveu veredito; se voltou `DESPACHÁVEL`, registrar o desvio de ordem em `changes.md` e despachar a T5.3 no worktree `gz-31`.
- **Depois**: onda 3 é a **T5.5** (camada `data`), que consome o contrato `BankConnectionRepository` (`getConnection`/`startConnection`/`disconnect`) da T5.4 e chama a função da T5.3; onda 4 é a **T5.6** (tela e rota `/configuracoes/banco`).
- **Por que a T5.3 pode andar sem a Pluggy**: o bloco DoD dela cobra que o handler leia as credenciais só de `Deno.env`, que nenhuma escrita use `SERVICE_ROLE` e que `handler_test.ts` prove `401`, `404` e `502` com o `fetch` **stubado**. Nenhuma linha exige credencial real — a ordem escrita no plano põe a T5.3 atrás da T5.2, e antecipá-la é desvio que exige registro em `changes.md` antes do despacho.
- **Pendências do humano**: **uma** — o par Client ID/Secret da **Pluggy** (**P13**), a ser criado na conta dele e registrado como env do Coolify e da stack local. Bloqueia a **T5.2** e, no fechamento da fase, a prova de integração contra a sandbox.
- **Armadilhas de ambiente já pagas** (repassar a todo executor): a política de RLS lida com `-U supabase_admin` sai **sem** o prefixo `auth.` — use `-e PGOPTIONS=--search_path=public` no `docker compose exec`; a stack Docker local é **única e compartilhada** entre a árvore principal e os worktrees, e `scripts/local-supabase.sh reset` faz `down -v`; heredoc para `docker exec -i psql` chega **vazio** e sai `exit 0`, use `< arquivo`; o Postgres está na **54322**; em worktree, histórico só por `rtk proxy git`; `flutter test -r compact` usa `\r`, passe por `tr '\r' '\n'` antes do `| tail`.
- **Régua que esta fase cobrou quatro vezes**: DoD só se prova com comando que **alcança o alvo certo**, **sob a role certa**, e que **falha sem o trabalho**. Grep negativo sobre pasta povoada é verde por construção; texto de política do Postgres depende do `search_path` de quem lê.
- **Ponteiros**: `docs/002_conta_e_configuracoes/03_plan.md` (Fase 5 a partir da linha 1404) · `changes.md` da feature (CHG-026 a CHG-029) · `docs/decisions.md` (**D24**, **P7**, **P8**) · provas da T5.1 em `docs/002_conta_e_configuracoes/provas/t5_1_provas_no_banco.md`.
