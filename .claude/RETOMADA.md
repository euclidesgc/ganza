# RETOMADA

- **Item em curso**: **007 - Agenda e Google Calendar** — Fases 1 e 2 mergeadas. A próxima é a **Fase 3 — agenda e cards no Flutter**, com as tarefas ainda `[ ]` em `docs/007_agenda/03_plan.md`; depois a Fase 4 (bateria, docs, fechamento).
- **Branch / PR**: nenhum aberto. `develop` limpa, só ela e `main`, sem worktrees e sem branch órfã.
- **Último gate/veredito**: Fase 2 fechada pelo PR #64 com os quatro gates em `pass` — `supervisor-dod` nas três tarefas (falsificação refeita por ele em cada uma), `ciso`, `qa` e `critico-integrador`. 219 testes no backend, 212 no app, todos os guards em `exit 0`.
- **Novidade que muda o fluxo**: **todo merge em `develop` que toque `app/**` ou `supabase/**` publica um APK assinado** (`.github/workflows/release.yml`, **D39**). Dois releases já saíram: `app-v0.1.0+1` e o do PR #64. A keystore de upload vive em `~/.ganza/` na máquina do humano e como secret no GitHub — **perdê-la custa a continuidade das atualizações**, não só um rebuild.
- **Primeira ação da retomada**: despachar o `auditor-de-criterios` sobre os blocos DoD da Fase 3 antes de qualquer execução, e rodar `bash scripts/lint-dod.sh docs/007_agenda/03_plan.md` a cada bloco novo.
- **Armadilha de critério que custou caro nesta sessão, não repita**: `deno test supabase/functions/...` **a partir da raiz** nunca passa — o import map de `supabase/functions/deno.json` é resolvido pelo diretório corrente, então o comando falha em `@std/assert` para qualquer implementação correta. Todo critério de teste Deno começa com `cd supabase/functions &&`. Ela passou por duas auditorias antes de ser pega.
- **O modo de falha que o `critico-integrador` pegou e os julgamentos de tarefa não pegam**: um critério de DoD que **reduz** silenciosamente uma decisão (a FD-005 mandava recusar por `recurringEventId` **ou** `recurrence`; o critério nomeou só o primeiro). Executor, supervisor e auditor acertam cada um o seu, e a lacuna só aparece na costura.
- **Pendências do humano**: **P1** — projeto Google Cloud, consent screen, redirect HTTPS e conta de teste; a Fase 3 segue com stub, mas **nada da 007 jamais falou com o Google de verdade**, e isso já escondeu um defeito real (FD-009). **P13** — credencial da Pluggy: o caminho recomendado é o **Meu Pluggy** (`meu.pluggy.ai`), gratuito para acessar as próprias contas, cujas chaves entram como `PLUGGY_CLIENT_ID`/`PLUGGY_CLIENT_SECRET` no serviço `ganza-supabase` do Coolify (exige redeploy). **P9** e **P10** seguem abertas.
- **Ponteiros**: `docs/007_agenda/03_plan.md` (Fase 3 em diante) · `docs/007_agenda/decisions.md` (FD-008 a FD-010) · `docs/007_agenda/changes.md` (CHG-001 a CHG-005) · `docs/decisions.md` (D39 publicação, D40 códigos de erro em inglês com gatilho de dívida).
